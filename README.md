数据本身不用清洗  
训练基础树模型    
探索性数据分析已完成，得到一些业务逻辑定义的新特征    
难样本分析，得到基于难样本定义的新特征   
使用PCA、Kmeans识别欺诈模式，无果  
进行特征交叉  
使用AutoEncoder、iForest交叉验证得到训练折外异常值得分，作为新特征，并对比二者效果  
进行特征消融，比较AUC-PR得到最终特征集

待处理：
网格搜索模型超参如树深、最大叶子树、缩放权重等等  
搜索阈值，得到最终F1_score  

注：两种版本，一种使用StratifiedKFold，随机交叉验证，但存在时间上的数据泄露。一种使用严格的时间序列验证purgedcv。但无论如何，数据集的数据已被PCA处理，本身就存在数据泄露
