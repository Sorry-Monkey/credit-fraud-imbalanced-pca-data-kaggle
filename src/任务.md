帮我整理出一个最终版。
这个最终版notebook应该：
1、先进行EDA（参考credit-fraud-eda），对于EDA，说明产出了什么特征（参考credit-fraud-feature-engineering-summary-report）
2、然后训练基线模型（什么额外特征都不加，分别使用StratifiedKFold和purgedcv验证作为基线，参考credit-fraud-eda和credit-fraud-feature-engineering-6）。对StratifiedKFold部分代码，说明其数据泄露的弊端，后续弃用
3、根据基线模型进行难样本分析（参考credit-fraud-eda）。对于难样本分析，应当说明他们产出了什么特征，并对之前的EDA特征做了哪些修改和取舍（参考credit-fraud-feature-engineering-summary-report）
4、进行欺诈模式/画像进行分析（代码参考credit-fraud-eda，说明额外参考credit-fraud-feature-engineering-summary-report）
5、测试focal loss（只留cv版本，不留非cv版本），但注释掉，不执行，并说明其效果和spw没什么区别。对于模糊样本加权，同样如此，说明其效果一般（参考credit-fraud-feature-engineering-7，但改用base特征组来做）。说明二者一个是对不均衡数据集的应对尝试，一个是防止正负样本排序虽好但分数接近，区分度不大导致模型难用的防范尝试
6、估计异常分作为新特征，分别使用Autoencoder和Isolation Forest，其中AE的代码注释掉，不执行，说明其效果一般且不如IF。（参考credit-fraud-feature-engineering-5和credit-fraud-feature-engineering-6）
7、第一次使用候选特征组进行特征消融，验证最优特征组合。purgedcv代码为主（参考credit-fraud-feature-engineering-3和credit-fraud-feature-engineering-6）。说明为什么以AUC-PR判断优劣（因为现实中银行的业务要求打扰率FPR<=0.1%在后来模型的训练结果中被证明已经自动满足了，以AUC-PR为主，F1为辅的挑选方式为模型提供了更高的上限，F1在最后模型落地时作为优化目标，AUC在训练过程中用来判断模型底色好坏）
8、说明最后如何选中的最优特征组`IF + hours_since_start + log1p_amount + A_top2`，选择办法参考credit-fraud-feature-engineering-summary-report。应该把这个特征组和之前的那些特征组整理到一起验证特征消融，不应该作为补测单独列出。
9、展示这个特征组调参后的最优参数和得到的各个特征的贡献度（直接使用最用参数得到特征贡献度即可，不用展示调参过程，因为后面还要再调一次，但要说明因为后面也有调参展示，这里就不赘述直接用最优参数来运行程序），并由此衍生出的新特征`v14_x_v10`, `v14_x_v4`, `v10_x_v4`, `abs_v14_minus_v10`, `v14_x_log1p_amount`（参考credit-fraud-feature-engineering-8）
10、先测试出最好的贡献度衍生出的新特征的最优组合，选其中效果前三的特征组，和老特征各种组合进行特征消融（参考credit-fraud-feature-engineering-8）。说明测试的特征组进行组合的规则，是如何组合出来的
11、展示最后的特征组`IF + hours + one_euro + micro + bands + FE8(abs_v14_minus_v10, v14_x_v10, v14_x_log1p_amount) + A_top2`对比base和`IF + hours_since_start + log1p_amount + A_top2`的提升
12、对最后的特征组进行调参，详细说明为什么选择以这个顺序进行调参，为什么以AUC为主，logloss和F1为辅判断参数好坏
13、使用最后两个树模型各自最优的超参和最后的特征组进行模型融合，尝试三种融合方式（参考credit-fraud-final-tune-stack）。通过图表比较它们的召回率、精准率确定最后最好的融合方式。说明rank时logloss由于概率被转为百分位排名（0~1）而不稳定的原因（其实是个假象，没什么参考意义），并给出逻辑回归效果不好的可能性分析


注：
1、以上所提到的被注释掉，不执行的代码，可以并非被真的用#或""""""注释到，而是默认不执行，但提供切换开关，可以通过这个开关切换执行并验证他们的效果。但他们不行，不选用他们的原因一定要写的显著
2、所有程序依赖的包应当在一开始的代码cell中就被导入，但所有的自己定义的方法和函数只有在用到时才写出，而非都堆在最前面的代码cell中。并且应该分离执行和准备，即应该使用一个单元格准备接下来马上要用的所有方法和函数（除非这个方法已经在更早之前的单元格中被定义过，此处只是复用），并在这个单元格下面再开一个单元格来执行，这样方便阅读
3、所有输出应该直接print在notebook中，并输出为文件保存在output文件夹中（文件名应该直观）。所有的输出，不管是直接print、display，还是存在文件里，都应该易读（特征消融表格的样式可以参考credit-fraud-feature-engineering-6，单元格23的输出。若特征太多，可以只展示超过base的部分或前15名。若随机种子太多，可以展示均值，而不每个种子各出一张表，但要展示结果的稳定性，比如deltaAUC是否正负翻转或有更好的别的展示办法）
4、所有的输出的图的标题、x轴单位、y轴单位等等都应该是中文
5、保留参考的代码中的中文注释（如果没有改动的话而直接复用的话）
